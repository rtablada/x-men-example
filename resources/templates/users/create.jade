extends /layouts/admin

block content
    form.col-lg-12(action=urlHelper.store() method="POST")
        include _form


        .btn-group
            a.btn.btn-default(href=urlHelper.index()) Back
            button.btn.btn-default(type="submit") Submit
